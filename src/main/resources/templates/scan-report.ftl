<#ftl output_format="plainText">

<----------- ******* ----------->

Scan completed:

Media source:   id=${mediaSource.id()}; name=${mediaSource.name()}; path = ${mediaSource.path()}
------------
files added:    ${counts.addedFilesCount}
files modified: ${counts.modifiedFilesCount}
files deleted:  ${counts.deletedFilesCount}
------------
Scan duration:  ${duration}

<----------- ******* ----------->